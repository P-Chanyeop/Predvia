<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Gumaedaehang.SettingsPage"
             Name="SettingsPageControl">

  <UserControl.Styles>
    <!-- 라이트모드 스타일 -->
    <Style Selector="UserControl">
      <Setter Property="Background" Value="White"/>
    </Style>
    
    <!-- 다크모드 스타일 -->
    <Style Selector="UserControl.dark-theme">
      <Setter Property="Background" Value="#1E1E1E"/>
    </Style>

    <!-- 메뉴 제목 버튼 스타일 - 라이트모드 -->
    <Style Selector="Button.menu-title-button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="HorizontalAlignment" Value="Left"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- 메뉴 제목 버튼 호버 스타일 - 라이트모드 -->
    <Style Selector="Button.menu-title-button:pointerover">
      <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- 메뉴 제목 버튼 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme Button.menu-title-button">
      <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- 메뉴 제목 버튼 호버 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme Button.menu-title-button:pointerover">
      <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- 메인 제목 스타일 - 라이트모드 (계정관리 - 주황색, 매우 큰 크기) -->
    <Style Selector="Button.main-title">
      <Setter Property="FontSize" Value="48"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="Foreground" Value="#E67E22"/>
      <Setter Property="Margin" Value="0,0,0,80"/>
    </Style>

    <!-- 메인 제목 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme Button.main-title">
      <Setter Property="Foreground" Value="#FFDAC4"/>
    </Style>

    <!-- 서브 제목 스타일 - 라이트모드 (마켓주소관리, 프로그램 설정 - 회색, 큰 크기) -->
    <Style Selector="Button.sub-title">
      <Setter Property="FontSize" Value="36"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Foreground" Value="#999999"/>
      <Setter Property="Margin" Value="0,0,0,60"/>
    </Style>

    <!-- 서브 제목 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme Button.sub-title">
      <Setter Property="Foreground" Value="#AAAAAA"/>
    </Style>

    <!-- 동적 위치 설정 박스 스타일 - 라이트모드 -->
    <Style Selector="Border.settings-box">
      <Setter Property="Background" Value="White"/>
      <Setter Property="BorderBrush" Value="#E67E22"/>
      <Setter Property="BorderThickness" Value="2"/>
      <Setter Property="CornerRadius" Value="15"/>
      <Setter Property="Padding" Value="40"/>
      <Setter Property="Width" Value="400"/>
      <Setter Property="Height" Value="320"/>
    </Style>

    <!-- 중앙 설정 박스 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme Border.settings-box">
      <Setter Property="Background" Value="#2D2D2D"/>
      <Setter Property="BorderBrush" Value="#FFDAC4"/>
    </Style>

    <!-- 메뉴 항목 버튼 스타일 - 라이트모드 (크기 증가) -->
    <Style Selector="Button.menu-item">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="#333333"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="FontSize" Value="20"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="0,20"/>
      <Setter Property="HorizontalAlignment" Value="Left"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="Width" Value="320"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- 메뉴 항목 버튼 호버 스타일 - 라이트모드 -->
    <Style Selector="Button.menu-item:pointerover">
      <Setter Property="Background" Value="#FFF5E6"/>
      <Setter Property="Foreground" Value="#E67E22"/>
    </Style>

    <!-- 메뉴 항목 버튼 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme Button.menu-item">
      <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- 메뉴 항목 버튼 호버 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme Button.menu-item:pointerover">
      <Setter Property="Background" Value="#3A3A3A"/>
      <Setter Property="Foreground" Value="#FFDAC4"/>
    </Style>

    <!-- PREDVIA 워터마크 스타일 - 라이트모드 (크기 증가) -->
    <Style Selector="TextBlock.watermark">
      <Setter Property="FontSize" Value="64"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="Foreground" Value="#F0F0F0"/>
      <Setter Property="Opacity" Value="0.3"/>
      <Setter Property="HorizontalAlignment" Value="Right"/>
      <Setter Property="VerticalAlignment" Value="Bottom"/>
      <Setter Property="Margin" Value="0,0,80,80"/>
    </Style>

    <!-- PREDVIA 워터마크 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme TextBlock.watermark">
      <Setter Property="Foreground" Value="#404040"/>
      <Setter Property="Opacity" Value="0.4"/>
    </Style>

    <!-- 인라인 UI 스타일 - 라이트모드 -->
    <Style Selector="TextBox">
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Padding" Value="12,8"/>
      <Setter Property="CornerRadius" Value="6"/>
    </Style>

    <!-- 인라인 UI 스타일 - 다크모드 -->
    <Style Selector="UserControl.dark-theme TextBox">
      <Setter Property="Background" Value="#4A4A4A"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="#FFDAC4"/>
    </Style>

    <Style Selector="UserControl.dark-theme ComboBox">
      <Setter Property="Background" Value="#4A4A4A"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="#FFDAC4"/>
    </Style>

    <Style Selector="UserControl.dark-theme TextBlock">
      <Setter Property="Foreground" Value="White"/>
    </Style>

    <Style Selector="UserControl.dark-theme Button">
      <Setter Property="Background" Value="#FFDAC4"/>
      <Setter Property="Foreground" Value="#333333"/>
    </Style>

    <Style Selector="UserControl.dark-theme Button:pointerover">
      <Setter Property="Background" Value="#FFE4C4"/>
    </Style>

    <!-- 새 페이지 다크모드 스타일 -->
    <Style Selector="UserControl.dark-theme Border#NicknameChangePage">
      <Setter Property="Background" Value="#2D2D2D"/>
      <Setter Property="BorderBrush" Value="#FFDAC4"/>
    </Style>

    <Style Selector="UserControl.dark-theme Border#MarketSetupPage">
      <Setter Property="Background" Value="#2D2D2D"/>
      <Setter Property="BorderBrush" Value="#FFDAC4"/>
    </Style>

    <!-- 다크모드 텍스트 색상 -->
    <Style Selector="UserControl.dark-theme Border#NicknameChangePage TextBlock">
      <Setter Property="Foreground" Value="White"/>
    </Style>

    <Style Selector="UserControl.dark-theme Border#MarketSetupPage TextBlock">
      <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- 다크모드 제목 색상 유지 -->
    <Style Selector="UserControl.dark-theme Border#NicknameChangePage > ScrollViewer > StackPanel > TextBlock:nth-child(1)">
      <Setter Property="Foreground" Value="#FFDAC4"/>
    </Style>

    <Style Selector="UserControl.dark-theme Border#MarketSetupPage > ScrollViewer > StackPanel > TextBlock:nth-child(1)">
      <Setter Property="Foreground" Value="#FFDAC4"/>
    </Style>

    <!-- 다크모드 입력 필드 -->
    <Style Selector="UserControl.dark-theme Border#NicknameChangePage TextBox">
      <Setter Property="Background" Value="#4A4A4A"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="#FFDAC4"/>
    </Style>

    <Style Selector="UserControl.dark-theme Border#MarketSetupPage TextBox">
      <Setter Property="Background" Value="#4A4A4A"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="#FFDAC4"/>
    </Style>

    <Style Selector="UserControl.dark-theme Border#MarketSetupPage ComboBox">
      <Setter Property="Background" Value="#4A4A4A"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="#FFDAC4"/>
    </Style>

    <!-- 다크모드 버튼 -->
    <Style Selector="UserControl.dark-theme Border#NicknameChangePage Button">
      <Setter Property="Background" Value="#FFDAC4"/>
      <Setter Property="Foreground" Value="#333333"/>
    </Style>

    <Style Selector="UserControl.dark-theme Border#MarketSetupPage Button">
      <Setter Property="Background" Value="#FFDAC4"/>
      <Setter Property="Foreground" Value="#333333"/>
    </Style>

    <!-- 다크모드 취소 버튼 -->
    <Style Selector="UserControl.dark-theme Border#NicknameChangePage Button:nth-child(2)">
      <Setter Property="Background" Value="#666666"/>
      <Setter Property="Foreground" Value="White"/>
    </Style>

    <Style Selector="UserControl.dark-theme Border#MarketSetupPage Button:nth-child(2)">
      <Setter Property="Background" Value="#666666"/>
      <Setter Property="Foreground" Value="White"/>
    </Style>

  </UserControl.Styles>

  <Grid>
    <!-- 메인 레이아웃 (15.png 비율에 맞춤) -->
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="500"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="400"/>
    </Grid.ColumnDefinitions>

    <!-- 왼쪽 영역 - 메뉴 제목들 (15.png와 정확히 일치하는 크기와 위치) -->
    <StackPanel Grid.Column="0" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Left"
                Margin="120,0,0,0"
                x:Name="MenuStackPanel">
      
      <!-- 계정관리 (클릭 가능한 버튼) -->
      <Button Classes="menu-title-button main-title" 
              Content="계정관리" 
              Click="OnAccountManagementClick"
              x:Name="AccountManagementButton"/>
      
      <!-- 동적 간격 1 -->
      <Border Height="0" x:Name="Spacer1"/>
      
      <!-- 마켓주소관리 (클릭 가능한 버튼) -->
      <Button Classes="menu-title-button sub-title" 
              Content="마켓주소관리" 
              Click="OnMarketAddressClick"
              x:Name="MarketAddressButton"/>
      
      <!-- 동적 간격 2 -->
      <Border Height="0" x:Name="Spacer2"/>
      
      <!-- 프로그램 설정 (클릭 가능한 버튼) - 주석 처리 -->
      <!--
      <Button Classes="menu-title-button sub-title" 
              Content="프로그램 설정" 
              Click="OnProgramSettingsClick"
              x:Name="ProgramSettingsButton"/>
      -->
      
    </StackPanel>

    <!-- 동적 위치 설정 박스 (Canvas로 절대 위치 지정) -->
    <Canvas Grid.Column="0" Grid.ColumnSpan="3" x:Name="SettingsCanvas">
      <!-- 계정관리용 설정 박스 -->
      <Border Classes="settings-box" 
              x:Name="AccountSettingsBox"
              Canvas.Left="520" 
              Canvas.Top="150"
              IsVisible="False">
        <StackPanel>
          
          <!-- 닉네임 변경 -->
          <Button Classes="menu-item" Content="닉네임 변경" Click="OnNicknameChangeClick"/>
          
          <Border Height="1" 
                  Background="#E0E0E0" 
                  Margin="0,10,0,10"/>
          
          <!-- 네이버 로그인 -->
          <Button Classes="menu-item" Content="네이버 로그인" Click="OnNaverLoginClick"/>
          
          <Border Height="1" 
                  Background="#E0E0E0" 
                  Margin="0,10,0,10"/>
          
          <!-- 타오바오 계정 연동 -->
          <Button Classes="menu-item" Content="타오바오 계정 연동" Click="OnTaobaoLoginClick"/>
          
        </StackPanel>
      </Border>

      <!-- 마켓주소관리용 설정 박스 (16.png와 100% 일치) -->
      <Border Classes="settings-box" 
              x:Name="MarketSettingsBox"
              Canvas.Left="520" 
              Canvas.Top="350"
              IsVisible="False">
        <StackPanel>
          
          <!-- 내마켓 설정하기 (16.png의 유일한 버튼) -->
          <Button Classes="menu-item" 
                  Content="내마켓 설정하기" 
                  Click="OnMarketSetupClick"
                  HorizontalAlignment="Center"
                  Margin="0,20,0,10"/>
          
        </StackPanel>
      </Border>

      <!-- 프로그램 설정용 설정 박스 - 주석 처리 -->
      <!--
      <Border Classes="settings-box" 
              x:Name="ProgramSettingsBox"
              Canvas.Left="520" 
              Canvas.Top="550"
              IsVisible="False">
        <StackPanel>
          
          <Button Classes="menu-item" Content="테마 설정" Click="OnThemeSettingsClick"/>
          
          <Border Height="1" 
                  Background="#E0E0E0" 
                  Margin="0,10,0,10"
                  x:Name="ProgramSeparator1"/>
          
          <Button Classes="menu-item" Content="언어 설정" Click="OnLanguageSettingsClick"/>
          
          <Border Height="1" 
                  Background="#E0E0E0" 
                  Margin="0,10,0,10"
                  x:Name="ProgramSeparator2"/>
          
          <Button Classes="menu-item" Content="알림 설정" Click="OnNotificationSettingsClick"/>
          
        </StackPanel>
      </Border>
      -->
    </Canvas>

    <!-- 우측 영역 - PREDVIA 워터마크 -->
    <Grid Grid.Column="2">
      
      <!-- PREDVIA 워터마크 (기본 상태) -->
      <TextBlock Classes="watermark" 
                 Text="PREDVIA"
                 x:Name="PredviaWatermark"/>

    </Grid>

    <!-- 전체 화면 오버레이 - 닉네임 변경 페이지 -->
    <Border x:Name="NicknameChangePage" 
            Grid.ColumnSpan="3"
            IsVisible="False"
            Background="White"
            BorderBrush="#E67E22"
            BorderThickness="4"
            CornerRadius="25"
            Padding="80"
            Margin="100,80,100,80"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ZIndex="1000">
      
      <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="800">
          
          <!-- 제목 -->
          <TextBlock Text="닉네임 변경" 
                     FontSize="48"
                     FontWeight="Bold"
                     Foreground="#E67E22"
                     HorizontalAlignment="Center"
                     Margin="0,0,0,60"/>
          
          <!-- 현재 닉네임 -->
          <TextBlock Text="현재 닉네임" 
                     FontSize="24" 
                     FontWeight="SemiBold" 
                     Foreground="#333333"
                     Margin="0,0,0,20"/>
          
          <TextBox x:Name="CurrentNicknameDisplay"
                   Text="사용자123"
                   IsReadOnly="True"
                   FontSize="24"
                   Padding="25,20"
                   Background="#F5F5F5"
                   Foreground="#333333"
                   BorderBrush="#E0E0E0"
                   BorderThickness="3"
                   CornerRadius="15"
                   Margin="0,0,0,40"/>
          
          <!-- 새 닉네임 -->
          <TextBlock Text="새 닉네임" 
                     FontSize="24" 
                     FontWeight="SemiBold" 
                     Foreground="#333333"
                     Margin="0,0,0,20"/>
          
          <TextBox x:Name="NewNicknameInput"
                   Watermark="새로운 닉네임을 입력하세요"
                   FontSize="24"
                   Padding="25,20"
                   Background="#FFDAC4"
                   Foreground="#333333"
                   BorderBrush="#E67E22"
                   BorderThickness="4"
                   CornerRadius="15"
                   MaxLength="20"
                   Margin="0,0,0,50"/>
          
          <!-- 버튼들 -->
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,40">
            <Button Content="변경" 
                    Click="OnNicknameSaveClick"
                    FontSize="24"
                    FontWeight="Bold"
                    Padding="50,25"
                    Background="#E67E22"
                    Foreground="White"
                    BorderThickness="0"
                    CornerRadius="20"
                    Margin="0,0,30,0"
                    Cursor="Hand"/>
            
            <Button Content="취소" 
                    Click="OnNicknameCancelClick"
                    FontSize="24"
                    FontWeight="Bold"
                    Padding="50,25"
                    Background="#CCCCCC"
                    Foreground="#333333"
                    BorderThickness="0"
                    CornerRadius="20"
                    Cursor="Hand"/>
          </StackPanel>
          
          <!-- 오류 메시지 -->
          <TextBlock x:Name="NicknameErrorMessage"
                     Text=""
                     FontSize="20"
                     Foreground="#E74C3C"
                     HorizontalAlignment="Center"
                     Margin="0,20,0,0"
                     IsVisible="False"/>
          
        </StackPanel>
      </ScrollViewer>
    </Border>

    <!-- 전체 화면 오버레이 - 마켓 설정 페이지 -->
    <Border x:Name="MarketSetupPage" 
            Grid.ColumnSpan="3"
            IsVisible="False"
            Background="White"
            BorderBrush="#E67E22"
            BorderThickness="4"
            CornerRadius="25"
            Padding="80"
            Margin="100,80,100,80"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ZIndex="1000">
      
      <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" MaxWidth="900">
          
          <!-- 제목 -->
          <TextBlock Text="내마켓 설정하기" 
                     FontSize="48"
                     FontWeight="Bold"
                     Foreground="#E67E22"
                     HorizontalAlignment="Center"
                     Margin="0,0,0,60"/>
          
          <!-- 마켓 이름 -->
          <TextBlock Text="📍 마켓 이름" 
                     FontSize="26" 
                     FontWeight="Bold" 
                     Foreground="#333333"
                     Margin="0,0,0,20"/>
          
          <TextBox x:Name="MarketNameInput"
                   Watermark="예: 홍길동의 쇼핑몰"
                   FontSize="24"
                   Padding="25,20"
                   Background="#FFDAC4"
                   Foreground="#333333"
                   BorderBrush="#E67E22"
                   BorderThickness="4"
                   CornerRadius="15"
                   Margin="0,0,0,40"/>
          
          <!-- 마켓 카테고리 -->
          <TextBlock Text="🏷️ 마켓 카테고리" 
                     FontSize="26" 
                     FontWeight="Bold" 
                     Foreground="#333333"
                     Margin="0,0,0,20"/>
          
          <ComboBox x:Name="MarketCategorySelect"
                    PlaceholderText="카테고리를 선택하세요"
                    FontSize="24"
                    Padding="25,20"
                    Background="#FFDAC4"
                    Foreground="#333333"
                    BorderBrush="#E67E22"
                    BorderThickness="4"
                    CornerRadius="15"
                    Margin="0,0,0,40">
            <ComboBoxItem Content="패션/의류"/>
            <ComboBoxItem Content="뷰티/화장품"/>
            <ComboBoxItem Content="생활용품"/>
            <ComboBoxItem Content="전자제품"/>
            <ComboBoxItem Content="스포츠/레저"/>
            <ComboBoxItem Content="기타"/>
          </ComboBox>
          
          <!-- 네이버 스토어 URL -->
          <TextBlock Text="🔗 네이버 쇼핑 스토어 URL" 
                     FontSize="26" 
                     FontWeight="Bold" 
                     Foreground="#333333"
                     Margin="0,0,0,20"/>
          
          <TextBox x:Name="NaverStoreUrlInput"
                   Watermark="https://smartstore.naver.com/yourstore"
                   FontSize="24"
                   Padding="25,20"
                   Background="#FFDAC4"
                   Foreground="#333333"
                   BorderBrush="#E67E22"
                   BorderThickness="4"
                   CornerRadius="15"
                   Margin="0,0,0,40"/>
          
          <!-- 기본 배송비 -->
          <TextBlock Text="🚚 기본 배송비" 
                     FontSize="26" 
                     FontWeight="Bold" 
                     Foreground="#333333"
                     Margin="0,0,0,20"/>
          
          <TextBox x:Name="ShippingFeeInput"
                   Text="3000"
                   Watermark="예: 3000"
                   FontSize="24"
                   Padding="25,20"
                   Background="#FFDAC4"
                   Foreground="#333333"
                   BorderBrush="#E67E22"
                   BorderThickness="4"
                   CornerRadius="15"
                   Margin="0,0,0,40"/>
          
          <!-- 무료배송 최소금액 -->
          <TextBlock Text="📦 무료배송 최소 주문금액" 
                     FontSize="26" 
                     FontWeight="Bold" 
                     Foreground="#333333"
                     Margin="0,0,0,20"/>
          
          <TextBox x:Name="FreeShippingMinInput"
                   Text="50000"
                   Watermark="예: 50000"
                   FontSize="24"
                   Padding="25,20"
                   Background="#FFDAC4"
                   Foreground="#333333"
                   BorderBrush="#E67E22"
                   BorderThickness="4"
                   CornerRadius="15"
                   Margin="0,0,0,50"/>
          
          <!-- 버튼들 -->
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,40">
            <Button Content="설정 저장" 
                    Click="OnMarketSaveClick"
                    FontSize="26"
                    FontWeight="Bold"
                    Padding="60,25"
                    Background="#E67E22"
                    Foreground="White"
                    BorderThickness="0"
                    CornerRadius="20"
                    Margin="0,0,30,0"
                    Cursor="Hand"/>
            
            <Button Content="취소" 
                    Click="OnMarketCancelClick"
                    FontSize="26"
                    FontWeight="Bold"
                    Padding="60,25"
                    Background="#CCCCCC"
                    Foreground="#333333"
                    BorderThickness="0"
                    CornerRadius="20"
                    Cursor="Hand"/>
          </StackPanel>
          
          <!-- 성공/오류 메시지 -->
          <TextBlock x:Name="MarketSuccessMessage"
                     Text=""
                     FontSize="22"
                     Foreground="#27AE60"
                     HorizontalAlignment="Center"
                     Margin="0,20,0,0"
                     IsVisible="False"/>
          
          <TextBlock x:Name="MarketErrorMessage"
                     Text=""
                     FontSize="22"
                     Foreground="#E74C3C"
                     HorizontalAlignment="Center"
                     Margin="0,20,0,0"
                     IsVisible="False"/>
          
        </StackPanel>
      </ScrollViewer>
    </Border>

  </Grid>

</UserControl>
